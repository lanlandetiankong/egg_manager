<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.egg.manager.mapper.user.UserAccountMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.egg.manager.entity.user.UserAccount">
        <id column="fid" property="fid" />
        <result column="user_name" property="userName" />
        <result column="account" property="account" />
        <result column="nick_name" property="nickName" />
        <result column="avatar_url" property="avatarUrl" />
        <result column="password" property="password" />
        <result column="phone" property="phone" />
        <result column="email" property="email" />
        <result column="sex" property="sex" />
        <result column="user_type" property="userType" />
        <result column="user_type_num" property="userTypeNum" />
        <result column="state" property="state" />
        <result column="version" property="version" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="create_user" property="createUser" />
        <result column="last_modifyer" property="lastModifyer" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        fid, user_name AS userName, account, nick_name AS nickName, avatar_url AS avatarUrl,password, phone, email, sex, user_type AS userType, type, state, version, create_time AS createTime, update_time AS updateTime,create_user AS createUser,last_modifyer AS lastModifyer
    </sql>


    <update id="batchFakeDelByIds" >
        UPDATE em_user_account SET  state = -1,last_modifyer = #{loginUser.fid}
        WHERE fid IN (
          <foreach collection="delIds" item="item" index="index" separator="," open="(" close=")">
              #{item}
          </foreach>
        )
    </update>
    <update id="batchLockUserByIds" >
        UPDATE em_user_account SET locked = #{lockState}
            <if test="loginUser != null">
                ,last_modifyer = #{loginUser.fid}
            </if>
        WHERE fid IN
            <foreach collection="lockIds" item="item" index="index" separator="," open="(" close=")">
                #{item}
            </foreach>
    </update>

    <update id="clearAllRoleByUserId" >
        UPDATE em_user_role
        SET state = -1
            <if test="loginUser != null">
                ,last_modifyer = #{loginUser.fid}
            </if>
        WHERE user_account_id = #{userAccountId}
    </update>


    <update id="clearAllJobByUserId">
        UPDATE em_user_job
        SET state = -1
            <if test="loginUser != null">
                ,last_modifyer = #{loginUser.fid}
            </if>
        WHERE user_account_id = #{userAccountId}
    </update>


</mapper>
